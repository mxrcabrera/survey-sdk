{
  "name": "surveyflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["sdk", "backend", "app"],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "cd app && npm install && npm run build"
  }
}